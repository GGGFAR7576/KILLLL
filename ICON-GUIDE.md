# 🎨 ИНСТРУКЦИЯ ПО ИКОНКАМ

## Текущее состояние

Создана SVG иконка в `assets/icon.svg` с военным дизайном:
- Hex-паттерн (шестиугольник)
- Щит с замком
- Цветовая схема проекта (#4a7c6f, #5a9d7e)

## Как создать PNG и ICO из SVG

### Вариант 1: Онлайн конвертеры

1. **Для PNG:**
   - Открой https://svgtopng.com/
   - Загрузи `assets/icon.svg`
   - Скачай как `icon.png` (рекомендуемый размер: 256x256 или 512x512)
   - Сохрани в `assets/icon.png`

2. **Для ICO:**
   - Открой https://convertio.co/png-ico/
   - Загрузи созданный `icon.png`
   - Скачай как `icon.ico`
   - Сохрани в `assets/icon.ico`

### Вариант 2: С помощью Node.js (если установлены зависимости)

```bash
# Установить зависимости
npm install --save-dev sharp png-to-ico

# Создать скрипт конвертации
node convert-icon.js
```

### Вариант 3: Использовать готовые инструменты

#### Windows:
- **IrfanView** (бесплатно): File → Save As → выбрать формат
- **GIMP** (бесплатно): File → Export As → выбрать `.ico`

#### macOS:
- **Preview**: File → Export → Format: PNG
- Для ICO использовать онлайн-конвертер

#### Linux:
```bash
# Установить ImageMagick
sudo apt install imagemagick

# Конвертировать SVG в PNG
convert assets/icon.svg -resize 256x256 assets/icon.png

# Конвертировать PNG в ICO
convert assets/icon.png -resize 256x256 assets/icon.ico
```

## Размеры для разных платформ

### Windows (Electron):
- **icon.ico**: 256x256 (основная)
- Также полезно: 16x16, 32x32, 48x48, 64x64, 128x128

### macOS (Electron):
- **icon.icns**: 512x512, 1024x1024
- Можно создать через: https://cloudconvert.com/png-to-icns

### Linux (Electron):
- **icon.png**: 512x512

## После создания иконок

1. Убедись что файлы не пустые (больше 0 байт)
2. Проверь что пути в `main.js` правильные:
   ```javascript
   icon: path.join(__dirname, 'assets', 'icon.png')
   ```
3. Проверь `package.json`:
   ```json
   "build": {
     "win": {
       "icon": "assets/icon.ico"
     }
   }
   ```

## Текущее использование

- ✅ SVG иконка в titlebar (встроенная)
- ⚠️ PNG нужна для окна приложения
- ⚠️ ICO нужна для сборки установщика

---

**Рекомендация:** Используй вариант 1 (онлайн конвертеры) - самый простой и быстрый способ!
