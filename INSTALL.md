# 🚀 БЫСТРАЯ УСТАНОВКА

## Шаг 1: Установка Node.js

1. Скачай Node.js: https://nodejs.org
2. Выбери **LTS версию** (рекомендуется)
3. Установи с настройками по умолчанию
4. Перезагрузи компьютер

## Шаг 2: Проверка установки

Открой **PowerShell** или **CMD** и введи:

```bash
node --version
npm --version
```

Должны показаться версии (например: `v20.10.0` и `10.2.3`)

## Шаг 3: Установка зависимостей

```bash
cd C:\Users\LOL11\CascadeProjects\viber-breach-system
npm install
```

Подожди 1-2 минуты пока установятся все пакеты.

## Шаг 4: Настройка IP-адреса

### 4.1 Узнай свой IP

Запусти сервер:
```bash
npm run server
```

Скопируй IP-адрес из вывода (например: `192.168.1.100`)

### 4.2 Обнови IP в файлах

**На НОУТБУКЕ:**
- Открой файл: `laptop/laptop-sender.js`
- Строка 10: `const SERVER_IP = '192.168.1.100';` ← **ТВОЙ IP**

**На ПК (если используешь standalone):**
- Открой файл: `pc/pc-receiver.js`
- Строка 11: `const SERVER_IP = '192.168.1.100';` ← **ТВОЙ IP**

## Шаг 5: Проверка путей к Viber

### Проверь что Viber установлен:

**Данные Viber:**
```
C:\Users\ТвойЮзер\AppData\Roaming\ViberPC
```

**Viber.exe:**
```
C:\Users\ТвойЮзер\AppData\Local\Viber\Viber.exe
```

### Если пути отличаются:

**Обнови в `main.js` (строки 10-12):**
```javascript
const VIBER_PATH = path.join(os.homedir(), 'AppData', 'Roaming', 'ViberPC');
const VIBER_BACKUP = path.join(os.homedir(), 'AppData', 'Roaming', 'ViberPC_BACKUP');
const VIBER_EXE = path.join(os.homedir(), 'AppData', 'Local', 'Viber', 'Viber.exe');
```

**Обнови в `pc/pc-receiver.js` (строки 14-16):**
```javascript
const VIBER_PATH = path.join(os.homedir(), 'AppData', 'Roaming', 'ViberPC');
const VIBER_BACKUP = path.join(os.homedir(), 'AppData', 'Roaming', 'ViberPC_BACKUP');
const VIBER_EXE = path.join(os.homedir(), 'AppData', 'Local', 'Viber', 'Viber.exe');
```

## Шаг 6: Первый запуск

### На ПК:

**Терминал 1 - Сервер:**
```bash
npm run server
```

**Терминал 2 - Приложение:**
```bash
npm start
```

### На НОУТБУКЕ:

**Терминал 1 - Отправитель:**
```bash
npm run laptop
```

## ✅ Готово!

Если всё работает:
- ✅ Сервер показывает "Waiting for connections..."
- ✅ Ноутбук показывает "Waiting for breach command from PC..."
- ✅ Приложение на ПК открылось
- ✅ После нажатия "ESTABLISH CONNECTION" статус стал "CONNECTED"

Теперь можешь снимать видео! 🎬

---

## ⚠️ Проблемы?

### "npm не является внутренней командой"
- Перезагрузи компьютер после установки Node.js
- Переоткрой терминал

### "Cannot find module 'ws'"
```bash
npm install
```

### "Connection failed"
- Проверь что сервер запущен
- Проверь IP-адрес (должен быть одинаковый)
- Проверь что оба устройства в одной WiFi сети
- Отключи Firewall или разреши порт 8080

### "Viber folder not found"
- Убедись что Viber установлен
- Запусти Viber хотя бы раз
- Залогинься в Viber
- Проверь путь в коде

### "Viber.exe not found"
- Найди где установлен Viber.exe
- Обнови путь в `main.js` и `pc/pc-receiver.js`

---

## 📞 Нужна помощь?

Проверь **README.md** для подробной инструкции.
